Create a modern, attractive, and highly functional Todo application frontend with the following specifications:

## Project Overview
Build a full-stack Todo application with FastAPI backend and React frontend. The frontend should be visually stunning, user-friendly, and feature-rich.

## Design Requirements

### Color Scheme & Theme
- Primary colors: Modern gradient from #667eea to #764ba2 (purple-blue)
- Secondary colors: Clean whites, subtle grays, and accent colors for actions
- Dark mode support with toggle
- Glassmorphism effects with backdrop blur
- Smooth color transitions and hover effects

### Layout & Structure
- Responsive design for mobile, tablet, and desktop
- Card-based layout with rounded corners and shadows
- Flexible grid system
- Proper spacing and typography hierarchy
- Sticky header with navigation
- Footer with app information

### Typography
- Modern font stack: 'Inter', 'Segoe UI', or system fonts
- Font sizes: 14px base, 16px for inputs, 18px for headings
- Font weights: 400 regular, 600 semibold, 700 bold
- Line heights: 1.5 for readability
- Text shadows for depth

## Component Specifications

### Header Component
- App title with logo/icon
- Dark mode toggle button
- User avatar/profile section
- Navigation breadcrumbs if needed
- Responsive hamburger menu for mobile

### Todo Form Component
- Clean input fields with floating labels
- Title field (required, max 100 chars)
- Description textarea (optional, max 500 chars)
- Priority selector (Low, Medium, High, Urgent)
- Category/tags input with autocomplete
- Due date picker with calendar
- File attachment support (images, documents)
- Submit button with loading state
- Form validation with error messages
- Keyboard shortcuts (Enter to submit)

### Todo List Component
- Filter and sort options (All, Active, Completed, Priority, Due Date)
- Search functionality with real-time filtering
- Bulk actions (select all, mark as done, delete selected)
- Pagination or infinite scroll for large lists
- Drag and drop for reordering
- Empty state with call-to-action

### Todo Item Component
- Checkbox with custom styling
- Title with strikethrough for completed items
- Description preview (expandable)
- Priority indicator (colored badges)
- Due date with overdue warnings
- Category tags
- Action buttons (edit, delete, duplicate)
- Progress indicator for multi-step todos
- Sub-tasks support
- Comments/Notes section

### Edit Modal/Dialog
- Modal overlay with backdrop
- All form fields from creation
- Save/Cancel buttons
- Keyboard shortcuts (Ctrl+S to save, Esc to cancel)
- Unsaved changes warning

## Advanced Features

### Animations & Interactions
- Page transitions with fade/slide effects
- Loading skeletons for better UX
- Micro-interactions on hover/click
- Success/error toast notifications
- Confetti animation for completing todos
- Smooth scrolling and parallax effects

### Data Visualization
- Progress charts (completed vs pending)
- Productivity analytics dashboard
- Calendar view for due dates
- Timeline view for todo history
- Statistics cards (total todos, completion rate, etc.)

### User Experience Enhancements
- Offline support with service workers
- Keyboard navigation support
- Voice input for accessibility
- Drag and drop file uploads
- Auto-save drafts
- Undo/redo functionality
- Export to PDF/CSV
- Import from other todo apps

### Performance Optimizations
- Code splitting and lazy loading
- Image optimization and lazy loading
- Virtual scrolling for large lists
- Memoization for expensive computations
- Efficient state management

## Technical Implementation

### Frontend Stack
- React 18 with hooks
- TypeScript for type safety
- Styled Components or CSS-in-JS
- React Router for navigation
- Redux Toolkit or Zustand for state management
- React Query for server state management
- Framer Motion for animations
- React Hook Form for form handling

### UI Library & Styling
- Tailwind CSS or Material-UI
- Custom design system
- CSS Grid and Flexbox
- Responsive breakpoints
- Dark mode with CSS variables
- Theming system

### Icons & Assets
- Lucide React or Heroicons
- Custom SVG icons
- Optimized images and illustrations
- Favicon and app icons
- Loading spinners and placeholders

## Backend Integration

### API Endpoints
- GET /todos - List todos with filtering
- POST /todos - Create new todo
- PUT /todos/{id} - Update todo
- DELETE /todos/{id} - Delete todo
- GET /todos/{id} - Get single todo
- POST /todos/{id}/complete - Mark as complete
- GET /stats - Get user statistics

### Error Handling
- Network error states
- Retry mechanisms
- Offline queue for actions
- Graceful degradation

## Accessibility (A11y)
- WCAG 2.1 AA compliance
- Screen reader support
- Keyboard navigation
- High contrast mode
- Focus management
- ARIA labels and roles

## Testing & Quality
- Unit tests for components
- Integration tests for user flows
- E2E tests with Cypress or Playwright
- Performance testing
- Accessibility testing
- Cross-browser testing

## Deployment & DevOps
- Docker containerization
- CI/CD pipeline
- Environment configurations
- Monitoring and logging
- CDN for assets
- SSL certificates

## Mobile Responsiveness
- Touch-friendly buttons (44px minimum)
- Swipe gestures for actions
- Pull-to-refresh
- Native app feel on mobile
- PWA capabilities

## Security Considerations
- Input sanitization
- XSS protection
- CSRF protection
- Rate limiting
- Secure headers

## Future Extensibility
- Plugin system for custom features
- API versioning
- Database migrations
- Feature flags
- A/B testing framework

## Code Quality
- ESLint and Prettier configuration
- Husky for git hooks
- Conventional commits
- Documentation with Storybook
- Code coverage reports

## Performance Metrics
- Lighthouse scores >90
- First Contentful Paint <1.5s
- Time to Interactive <3s
- Bundle size <200KB
- 100% accessibility score

## User Onboarding
- Welcome tour
- Tooltips for features
- Progressive disclosure
- Sample data for new users
- Help documentation

## Analytics & Tracking
- User behavior tracking
- Conversion funnels
- Error monitoring
- Performance monitoring
- A/B test results

## Internationalization (i18n)
- Multi-language support
- RTL language support
- Date/time localization
- Number formatting
- Cultural adaptations

## Collaboration Features
- Share todos with others
- Comments on todos
- Real-time updates
- Team workspaces
- Permission management

## Integration Options
- Calendar integration (Google, Outlook)
- Email notifications
- Slack/Discord webhooks
- Zapier integrations
- API for third-party apps

## Backup & Recovery
- Automatic backups
- Data export options
- Account recovery
- GDPR compliance
- Data retention policies

## Support & Documentation
- In-app help
- FAQ section
- Video tutorials
- Community forum
- Live chat support

## Monetization (Future)
- Freemium model
- Premium features
- Team plans
- Enterprise solutions
- White-label options

## Scalability Considerations
- Horizontal scaling
- Database optimization
- Caching strategies
- CDN integration
- Microservices architecture

## Monitoring & Alerting
- Application monitoring
- Error tracking
- Performance monitoring
- User analytics
- Business metrics

## Compliance & Legal
- Privacy policy
- Terms of service
- Cookie policy
- Data processing agreements
- Security audits

## Development Workflow
- Git flow branching strategy
- Code review process
- Automated testing
- Deployment automation
- Rollback procedures

## Quality Assurance
- Manual testing checklists
- Automated test suites
- User acceptance testing
- Beta testing program
- Bug bounty program

## Documentation
- API documentation
- User guides
- Developer guides
- Architecture diagrams
- Deployment guides

## Community & Open Source
- GitHub repository
- Contributing guidelines
- Code of conduct
- Issue templates
- Pull request templates

## Roadmap & Planning
- Feature prioritization
- Sprint planning
- Release planning
- User feedback integration
- Competitive analysis

## Risk Management
- Security risk assessment
- Technical debt management
- Dependency management
- Backup and disaster recovery
- Business continuity planning

## Team Structure
- Development team roles
- Design team integration
- Product management
- Quality assurance
- DevOps and infrastructure

## Communication
- Internal documentation
- Team communication tools
- Client communication
- Stakeholder updates
- Status reports

## Budget & Resources
- Development cost estimation
- Resource allocation
- Tool and software licenses
- Cloud infrastructure costs
- Maintenance and support costs

## Success Metrics
- User acquisition
- User retention
- Feature adoption
- Performance metrics
- Customer satisfaction

## Continuous Improvement
- User feedback loops
- Analytics-driven development
- Performance optimization
- Security updates
- Feature enhancements

## Technology Stack Details

### Frontend Technologies
- React 18.2.0
- TypeScript 4.9.5
- Vite 4.3.9 (build tool)
- Tailwind CSS 3.3.0
- Framer Motion 10.12.4
- React Hook Form 7.43.9
- React Query 4.29.7
- React Router 6.8.1
- Lucide React 0.244.0 (icons)
- Date-fns 2.29.3
- Axios 1.3.4

### Development Tools
- ESLint 8.35.0
- Prettier 2.8.4
- Husky 8.0.3
- Commitlint 17.4.4
- Storybook 7.0.0
- Cypress 12.7.0
- Jest 29.5.0
- React Testing Library 13.4.0

### Build & Deployment
- Docker 23.0.1
- GitHub Actions
- Vercel/Netlify
- AWS/GCP/Azure
- Cloudflare CDN

## Implementation Steps

1. Set up project structure
2. Implement basic components
3. Add styling and theming
4. Integrate with backend API
5. Add advanced features
6. Implement testing
7. Performance optimization
8. Deployment and monitoring

## Code Examples

### Basic Todo Component Structure
```tsx
interface Todo {
  id: string;
  title: string;
  description?: string;
  completed: boolean;
  priority: 'low' | 'medium' | 'high' | 'urgent';
  dueDate?: Date;
  createdAt: Date;
  updatedAt: Date;
}

interface TodoItemProps {
  todo: Todo;
  onToggle: (id: string) => void;
  onEdit: (id: string) => void;
  onDelete: (id: string) => void;
}

const TodoItem: React.FC<TodoItemProps> = ({ todo, onToggle, onEdit, onDelete }) => {
  return (
    <div className="todo-item">
      <input
        type="checkbox"
        checked={todo.completed}
        onChange={() => onToggle(todo.id)}
      />
      <div className="todo-content">
        <h3 className={todo.completed ? 'completed' : ''}>{todo.title}</h3>
        {todo.description && <p>{todo.description}</p>}
        <div className="todo-meta">
          <span className={`priority priority-${todo.priority}`}>{todo.priority}</span>
          {todo.dueDate && <span className="due-date">{format(todo.dueDate, 'PPP')}</span>}
        </div>
      </div>
      <div className="todo-actions">
        <button onClick={() => onEdit(todo.id)}>Edit</button>
        <button onClick={() => onDelete(todo.id)}>Delete</button>
      </div>
    </div>
  );
};
```

### Styled Components Example
```tsx
const TodoContainer = styled.div`
  background: rgba(255, 255, 255, 0.95);
  border-radius: 15px;
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
  padding: 30px;
  margin: 20px auto;
  max-width: 800px;
  backdrop-filter: blur(10px);
  transition: transform 0.3s ease;

  &:hover {
    transform: translateY(-5px);
  }
`;

const TodoTitle = styled.h3`
  color: #333;
  margin-bottom: 10px;
  font-weight: 600;
  
  &.completed {
    text-decoration: line-through;
    color: #888;
  }
`;

const PriorityBadge = styled.span<{ priority: string }>`
  padding: 4px 8px;
  border-radius: 12px;
  font-size: 0.8em;
  font-weight: 600;
  text-transform: uppercase;
  
  ${props => {
    switch (props.priority) {
      case 'urgent':
        return 'background: #dc3545; color: white;';
      case 'high':
        return 'background: #fd7e14; color: white;';
      case 'medium':
        return 'background: #ffc107; color: #212529;';
      case 'low':
        return 'background: #28a745; color: white;';
      default:
        return 'background: #6c757d; color: white;';
    }
  }}
`;
```

### Form Validation Schema
```tsx
import { z } from 'zod';

export const todoSchema = z.object({
  title: z.string()
    .min(1, 'Title is required')
    .max(100, 'Title must be less than 100 characters'),
  description: z.string()
    .max(500, 'Description must be less than 500 characters')
    .optional(),
  priority: z.enum(['low', 'medium', 'high', 'urgent']),
  dueDate: z.date().optional(),
  completed: z.boolean().default(false),
});

export type TodoFormData = z.infer<typeof todoSchema>;
```

### API Integration
```tsx
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import axios from 'axios';

const api = axios.create({
  baseURL: process.env.REACT_APP_API_URL || 'http://localhost:8000',
});

export const useTodos = (filters?: TodoFilters) => {
  return useQuery({
    queryKey: ['todos', filters],
    queryFn: async () => {
      const params = new URLSearchParams();
      if (filters?.status) params.append('status', filters.status);
      if (filters?.priority) params.append('priority', filters.priority);
      
      const response = await api.get(`/todos?${params}`);
      return response.data;
    },
  });
};

export const useCreateTodo = () => {
  const queryClient = useQueryClient();
  
  return useMutation({
    mutationFn: (todo: Omit<Todo, 'id' | 'createdAt' | 'updatedAt'>) => 
      api.post('/todos', todo),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['todos'] });
    },
  });
};

export const useUpdateTodo = () => {
  const queryClient = useQueryClient();
  
  return useMutation({
    mutationFn: ({ id, ...updates }: { id: string } & Partial<Todo>) => 
      api.put(`/todos/${id}`, updates),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['todos'] });
    },
  });
};

export const useDeleteTodo = () => {
  const queryClient = useQueryClient();
  
  return useMutation({
    mutationFn: (id: string) => api.delete(`/todos/${id}`),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['todos'] });
    },
  });
};
```

### Animation Examples
```tsx
import { motion } from 'framer-motion';

const TodoItem = ({ todo, onToggle, onEdit, onDelete }) => {
  return (
    <motion.div
      initial={{ opacity: 0, y: 20 }}
      animate={{ opacity: 1, y: 0 }}
      exit={{ opacity: 0, y: -20 }}
      transition={{ duration: 0.3 }}
      className="todo-item"
      whileHover={{ scale: 1.02 }}
      whileTap={{ scale: 0.98 }}
    >
      {/* Todo content */}
    </motion.div>
  );
};

const TodoList = ({ todos }) => {
  return (
    <motion.div
      initial="hidden"
      animate="visible"
      variants={{
        hidden: { opacity: 0 },
        visible: {
          opacity: 1,
          transition: {
            staggerChildren: 0.1,
          },
        },
      }}
    >
      {todos.map(todo => (
        <TodoItem key={todo.id} todo={todo} />
      ))}
    </motion.div>
  );
};
```

### Responsive Design
```css
.todo-container {
  display: grid;
  grid-template-columns: 1fr;
  gap: 20px;
  padding: 20px;
}

@media (min-width: 768px) {
  .todo-container {
    grid-template-columns: repeat(2, 1fr);
  }
}

@media (min-width: 1024px) {
  .todo-container {
    grid-template-columns: repeat(3, 1fr);
  }
}

.todo-item {
  background: white;
  border-radius: 12px;
  padding: 20px;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  transition: all 0.3s ease;
}

@media (max-width: 767px) {
  .todo-item {
    padding: 15px;
  }
  
  .todo-actions {
    flex-direction: column;
    gap: 8px;
  }
}
```

### Dark Mode Implementation
```tsx
import { createContext, useContext, useEffect, useState } from 'react';

type Theme = 'light' | 'dark';

interface ThemeContextType {
  theme: Theme;
  toggleTheme: () => void;
}

const ThemeContext = createContext<ThemeContextType | undefined>(undefined);

export const ThemeProvider: React.FC<{ children: React.ReactNode }> = ({ children }) => {
  const [theme, setTheme] = useState<Theme>(() => {
    const saved = localStorage.getItem('theme');
    return (saved as Theme) || 'light';
  });

  useEffect(() => {
    document.documentElement.setAttribute('data-theme', theme);
    localStorage.setItem('theme', theme);
  }, [theme]);

  const toggleTheme = () => {
    setTheme(prev => prev === 'light' ? 'dark' : 'light');
  };

  return (
    <ThemeContext.Provider value={{ theme, toggleTheme }}>
      {children}
    </ThemeContext.Provider>
  );
};

export const useTheme = () => {
  const context = useContext(ThemeContext);
  if (!context) throw new Error('useTheme must be used within ThemeProvider');
  return context;
};
```

### CSS Variables for Theming
```css
:root {
  --bg-primary: #ffffff;
  --bg-secondary: #f8f9fa;
  --text-primary: #333333;
  --text-secondary: #666666;
  --border-color: #e1e5e9;
  --shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  --gradient-primary: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  --gradient-secondary: linear-gradient(45deg, #f093fb 0%, #f5576c 100%);
}

[data-theme="dark"] {
  --bg-primary: #1a1a1a;
  --bg-secondary: #2d2d2d;
  --text-primary: #ffffff;
  --text-secondary: #cccccc;
  --border-color: #404040;
  --shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
}

.todo-item {
  background: var(--bg-primary);
  color: var(--text-primary);
  border: 1px solid var(--border-color);
  box-shadow: var(--shadow);
}
```

### Accessibility Features
```tsx
const TodoItem = ({ todo, onToggle, onEdit, onDelete }) => {
  const checkboxId = `todo-${todo.id}`;
  const titleId = `title-${todo.id}`;
  
  return (
    <article 
      className="todo-item"
      aria-labelledby={titleId}
      role="listitem"
    >
      <div className="todo-checkbox">
        <input
          id={checkboxId}
          type="checkbox"
          checked={todo.completed}
          onChange={() => onToggle(todo.id)}
          aria-describedby={`${checkboxId}-label`}
        />
        <label 
          id={`${checkboxId}-label`}
          htmlFor={checkboxId}
          className="visually-hidden"
        >
          Mark "{todo.title}" as {todo.completed ? 'incomplete' : 'complete'}
        </label>
      </div>
      
      <div className="todo-content">
        <h3 id={titleId} className={todo.completed ? 'completed' : ''}>
          {todo.title}
        </h3>
        {todo.description && (
          <p aria-label={`Description: ${todo.description}`}>
            {todo.description}
          </p>
        )}
        <div className="todo-meta" role="group" aria-label="Todo metadata">
          <span 
            className={`priority priority-${todo.priority}`}
            role="status"
            aria-label={`Priority: ${todo.priority}`}
          >
            {todo.priority}
          </span>
          {todo.dueDate && (
            <time 
              dateTime={todo.dueDate.toISOString()}
              aria-label={`Due date: ${format(todo.dueDate, 'PPP')}`}
            >
              {format(todo.dueDate, 'PPP')}
            </time>
          )}
        </div>
      </div>
      
      <div className="todo-actions" role="group" aria-label="Todo actions">
        <button 
          onClick={() => onEdit(todo.id)}
          aria-label={`Edit todo: ${todo.title}`}
        >
          Edit
        </button>
        <button 
          onClick={() => onDelete(todo.id)}
          aria-label={`Delete todo: ${todo.title}`}
        >
          Delete
        </button>
      </div>
    </article>
  );
};
```

### Performance Optimization
```tsx
import React, { memo, useMemo, useCallback } from 'react';

const TodoItem = memo(({ todo, onToggle, onEdit, onDelete }) => {
  const handleToggle = useCallback(() => {
    onToggle(todo.id);
  }, [onToggle, todo.id]);
  
  const handleEdit = useCallback(() => {
    onEdit(todo.id);
  }, [onEdit, todo.id]);
  
  const handleDelete = useCallback(() => {
    onDelete(todo.id);
  }, [onDelete, todo.id]);
  
  const formattedDate = useMemo(() => {
    return todo.dueDate ? format(todo.dueDate, 'PPP') : null;
  }, [todo.dueDate]);
  
  const isOverdue = useMemo(() => {
    return todo.dueDate && todo.dueDate < new Date() && !todo.completed;
  }, [todo.dueDate, todo.completed]);
  
  return (
    <div className={`todo-item ${isOverdue ? 'overdue' : ''}`}>
      {/* Todo content */}
    </div>
  );
});

TodoItem.displayName = 'TodoItem';
```

### Error Boundaries
```tsx
import React from 'react';

class ErrorBoundary extends React.Component {
  constructor(props) {
    super(props);
    this.state = { hasError: false, error: null };
  }

  static getDerivedStateFromError(error) {
    return { hasError: true, error };
  }

  componentDidCatch(error, errorInfo) {
    console.error('Todo app error:', error, errorInfo);
    // Log to error reporting service
  }

  render() {
    if (this.state.hasError) {
      return (
        <div className="error-boundary">
          <h2>Something went wrong</h2>
          <p>We're sorry, but something unexpected happened.</p>
          <button onClick={() => this.setState({ hasError: false, error: null })}>
            Try again
          </button>
          {process.env.NODE_ENV === 'development' && (
            <details>
              <summary>Error details</summary>
              <pre>{this.state.error?.toString()}</pre>
            </details>
          )}
        </div>
      );
    }

    return this.props.children;
  }
}

export default ErrorBoundary;
```

### Loading States
```tsx
const TodoList = () => {
  const { data: todos, isLoading, error } = useTodos();
  
  if (isLoading) {
    return (
      <div className="todo-list">
        {Array.from({ length: 5 }).map((_, index) => (
          <div key={index} className="todo-item skeleton">
            <div className="skeleton-checkbox"></div>
            <div className="skeleton-content">
              <div className="skeleton-title"></div>
              <div className="skeleton-description"></div>
              <div className="skeleton-meta"></div>
            </div>
            <div className="skeleton-actions">
              <div className="skeleton-button"></div>
              <div className="skeleton-button"></div>
            </div>
          </div>
        ))}
      </div>
    );
  }
  
  if (error) {
    return (
      <div className="error-state">
        <h3>Failed to load todos</h3>
        <p>{error.message}</p>
        <button onClick={() => window.location.reload()}>
          Retry
        </button>
      </div>
    );
  }
  
  return (
    <div className="todo-list">
      {todos?.map(todo => (
        <TodoItem key={todo.id} todo={todo} />
      ))}
    </div>
  );
};
```

### CSS for Loading Skeletons
```css
.skeleton {
  background: linear-gradient(90deg, #f0f0f0 25%, #e0e0e0 50%, #f0f0f0 75%);
  background-size: 200% 100%;
  animation: loading 1.5s infinite;
}

@keyframes loading {
  0% {
    background-position: 200% 0;
  }
  100% {
    background-position: -200% 0;
  }
}

.skeleton-checkbox {
  width: 20px;
  height: 20px;
  border-radius: 4px;
}

.skeleton-title {
  height: 24px;
  border-radius: 4px;
  margin-bottom: 8px;
}

.skeleton-description {
  height: 16px;
  border-radius: 4px;
  margin-bottom: 8px;
}

.skeleton-meta {
  height: 14px;
  width: 60px;
  border-radius: 12px;
}

.skeleton-button {
  height: 32px;
  width: 60px;
  border-radius: 6px;
  margin-left: 8px;
}
```

### Toast Notifications
```tsx
import { createContext, useContext, useState, useCallback } from 'react';
import { motion, AnimatePresence } from 'framer-motion';

type ToastType = 'success' | 'error' | 'warning' | 'info';

interface Toast {
  id: string;
  type: ToastType;
  title: string;
  message?: string;
  duration?: number;
}

interface ToastContextType {
  addToast: (toast: Omit<Toast, 'id'>) => void;
  removeToast: (id: string) => void;
}

const ToastContext = createContext<ToastContextType | undefined>(undefined);

export const ToastProvider: React.FC<{ children: React.ReactNode }> = ({ children }) => {
  const [toasts, setToasts] = useState<Toast[]>([]);
  
  const addToast = useCallback((toast: Omit<Toast, 'id'>) => {
    const id = Date.now().toString();
    const newToast = { ...toast, id };
    
    setToasts(prev => [...prev, newToast]);
    
    if (toast.duration !== 0) {
      setTimeout(() => {
        removeToast(id);
      }, toast.duration || 5000);
    }
  }, []);
  
  const removeToast = useCallback((id: string) => {
    setToasts(prev => prev.filter(toast => toast.id !== id));
  }, []);
  
  return (
    <ToastContext.Provider value={{ addToast, removeToast }}>
      {children}
      <ToastContainer toasts={toasts} removeToast={removeToast} />
    </ToastContext.Provider>
  );
};

const ToastContainer: React.FC<{ 
  toasts: Toast[]; 
  removeToast: (id: string) => void; 
}> = ({ toasts, removeToast }) => {
  return (
    <div className="toast-container">
      <AnimatePresence>
        {toasts.map(toast => (
          <motion.div
            key={toast.id}
            initial={{ opacity: 0, x: 300 }}
            animate={{ opacity: 1, x: 0 }}
            exit={{ opacity: 0, x: 300 }}
            className={`toast toast-${toast.type}`}
          >
            <div className="toast-content">
              <h4 className="toast-title">{toast.title}</h4>
              {toast.message && <p className="toast-message">{toast.message}</p>}
            </div>
            <button 
              className="toast-close"
              onClick={() => removeToast(toast.id)}
              aria-label="Close notification"
            >
              ×
            </button>
          </motion.div>
        ))}
      </AnimatePresence>
    </div>
  );
};

export const useToast = () => {
  const context = useContext(ToastContext);
  if (!context) throw new Error('useToast must be used within ToastProvider');
  return context;
};
```

### Search and Filter
```tsx
import { useState, useMemo } from 'react';
import { debounce } from 'lodash';

const TodoFilters = ({ onFiltersChange }) => {
  const [search, setSearch] = useState('');
  const [status, setStatus] = useState('all');
  const [priority, setPriority] = useState('all');
  const [sortBy, setSortBy] = useState('createdAt');
  const [sortOrder, setSortOrder] = useState('desc');
  
  const debouncedSearch = useMemo(
    () => debounce((value) => {
      onFiltersChange({ search: value, status, priority, sortBy, sortOrder });
    }, 300),
    [onFiltersChange, status, priority, sortBy, sortOrder]
  );
  
  const handleSearchChange = (e) => {
    const value = e.target.value;
    setSearch(value);
    debouncedSearch(value);
  };
  
  const handleFilterChange = (filterType, value) => {
    const newFilters = { search, status, priority, sortBy, sortOrder };
    newFilters[filterType] = value;
    
    switch (filterType) {
      case 'status':
        setStatus(value);
        break;
      case 'priority':
        setPriority(value);
        break;
      case 'sortBy':
        setSortBy(value);
        break;
      case 'sortOrder':
        setSortOrder(value);
        break;
    }
    
    onFiltersChange(newFilters);
  };
  
  return (
    <div className="todo-filters">
      <div className="search-container">
        <input
          type="text"
          placeholder="Search todos..."
          value={search}
          onChange={handleSearchChange}
          className="search-input"
        />
      </div>
      
      <div className="filter-controls">
        <select 
          value={status} 
          onChange={(e) => handleFilterChange('status', e.target.value)}
        >
          <option value="all">All Status</option>
          <option value="active">Active</option>
          <option value="completed">Completed</option>
        </select>
        
        <select 
          value={priority} 
          onChange={(e) => handleFilterChange('priority', e.target.value)}
        >
          <option value="all">All Priorities</option>
          <option value="urgent">Urgent</option>
          <option value="high">High</option>
          <option value="medium">Medium</option>
          <option value="low">Low</option>
        </select>
        
        <select 
          value={sortBy} 
          onChange={(e) => handleFilterChange('sortBy', e.target.value)}
        >
          <option value="createdAt">Created Date</option>
          <option value="dueDate">Due Date</option>
          <option value="priority">Priority</option>
          <option value="title">Title</option>
        </select>
        
        <button 
          onClick={() => handleFilterChange('sortOrder', sortOrder === 'asc' ? 'desc' : 'asc')}
          className="sort-toggle"
        >
          {sortOrder === 'asc' ? '↑' : '↓'}
        </button>
      </div>
    </div>
  );
};
```

### Drag and Drop
```tsx
import { useState } from 'react';
import { DragDropContext, Droppable, Draggable } from 'react-beautiful-dnd';

const TodoList = ({ todos, onReorder }) => {
  const [isDragDisabled, setIsDragDisabled] = useState(false);
  
  const handleDragEnd = (result) => {
    if (!result.destination) return;
    
    const items = Array.from(todos);
    const [reorderedItem] = items.splice(result.source.index, 1);
    items.splice(result.destination.index, 0, reorderedItem);
    
    onReorder(items);
  };
  
  return (
    <DragDropContext onDragEnd={handleDragEnd}>
      <Droppable droppableId="todos">
        {(provided) => (
          <div 
            className="todo-list"
            {...provided.droppableProps}
            ref={provided.innerRef}
          >
            {todos.map((todo, index) => (
              <Draggable 
                key={todo.id} 
                draggableId={todo.id} 
                index={index}
                isDragDisabled={isDragDisabled}
              >
                {(provided, snapshot) => (
                  <div
                    ref={provided.innerRef}
                    {...provided.draggableProps}
                    {...provided.dragHandleProps}
                    className={`todo-item ${snapshot.isDragging ? 'dragging' : ''}`}
                    style={{
                      ...provided.draggableProps.style,
                    }}
                  >
                    <div className="drag-handle">⋮⋮</div>
                    <TodoItem todo={todo} />
                  </div>
                )}
              </Draggable>
            ))}
            {provided.placeholder}
          </div>
        )}
      </Droppable>
    </DragDropContext>
  );
};
```

### CSS for Drag and Drop
```css
.drag-handle {
  cursor: grab;
  padding: 8px;
  color: #999;
  font-size: 14px;
  user-select: none;
}

.drag-handle:active {
  cursor: grabbing;
}

.todo-item.dragging {
  box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
  transform: rotate(5deg);
  opacity: 0.8;
}

.todo-list {
  min-height: 200px;
}
```

### PWA Features
```tsx
// public/manifest.json
{
  "name": "Todo App",
  "short_name": "Todo",
  "description": "A modern, beautiful todo application",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#667eea",
  "theme_color": "#667eea",
  "icons": [
    {
      "src": "/icon-192.png",
      "sizes": "192x192",
      "type": "image/png"
    },
    {
      "src": "/icon-512.png",
      "sizes": "512x512",
      "type": "image/png"
    }
  ]
}

// service-worker.js
const CACHE_NAME = 'todo-app-v1';
const urlsToCache = [
  '/',
  '/static/js/bundle.js',
  '/static/css/main.css',
  '/manifest.json'
];

self.addEventListener('install', (event) => {
  event.waitUntil(
    caches.open(CACHE_NAME)
      .then((cache) => cache.addAll(urlsToCache))
  );
});

self.addEventListener('fetch', (event) => {
  event.respondWith(
    caches.match(event.request)
      .then((response) => {
        if (response) {
          return response;
        }
        return fetch(event.request);
      }
    )
  );
});
```

### Offline Support
```tsx
import { useEffect } from 'react';
import { useToast } from './ToastContext';

const useOfflineSync = () => {
  const { addToast } = useToast();
  const [isOnline, setIsOnline] = useState(navigator.onLine);
  
  useEffect(() => {
    const handleOnline = () => {
      setIsOnline(true);
      addToast({
        type: 'success',
        title: 'Back Online',
        message: 'Your changes will be synced.',
        duration: 3000
      });
    };
    
    const handleOffline = () => {
      setIsOnline(false);
      addToast({
        type: 'warning',
        title: 'Offline Mode',
        message: 'Changes will be saved locally.',
        duration: 0
      });
    };
    
    window.addEventListener('online', handleOnline);
    window.addEventListener('offline', handleOffline);
    
    return () => {
      window.removeEventListener('online', handleOnline);
      window.removeEventListener('offline', handleOffline);
    };
  }, [addToast]);
  
  return isOnline;
};

// IndexedDB for offline storage
const dbPromise = indexedDB.open('todo-app', 1);

dbPromise.onupgradeneeded = (event) => {
  const db = event.target.result;
  if (!db.objectStoreNames.contains('todos')) {
    db.createObjectStore('todos', { keyPath: 'id' });
  }
  if (!db.objectStoreNames.contains('pendingActions')) {
    db.createObjectStore('pendingActions', { keyPath: 'id', autoIncrement: true });
  }
};

export const saveTodoOffline = async (todo) => {
  const db = await dbPromise;
  const tx = db.transaction('todos', 'readwrite');
  const store = tx.objectStore('todos');
  await store.put(todo);
};

export const getOfflineTodos = async () => {
  const db = await dbPromise;
  const tx = db.transaction('todos', 'readonly');
  const store = tx.objectStore('todos');
  return store.getAll();
};

export const syncOfflineData = async () => {
  const offlineTodos = await getOfflineTodos();
  const pendingActions = await getPendingActions();
  
  // Sync todos
  for (const todo of offlineTodos) {
    try {
      await api.post('/todos', todo);
    } catch (error) {
      console.error('Failed to sync todo:', error);
    }
  }
  
  // Execute pending actions
  for (const action of pendingActions) {
    try {
      await executePendingAction(action);
    } catch (error) {
      console.error('Failed to execute pending action:', error);
    }
  }
};
```

### Analytics Integration
```tsx
import { useEffect } from 'react';
import { useLocation } from 'react-router-dom';

// Google Analytics
const GA_TRACKING_ID = process.env.REACT_APP_GA_TRACKING_ID;

const useAnalytics = () => {
  const location = useLocation();
  
  useEffect(() => {
    if (GA_TRACKING_ID) {
      // Track page views
      gtag('config', GA_TRACKING_ID, {
        page_path: location.pathname,
      });
    }
  }, [location]);
  
  const trackEvent = (eventName, parameters = {}) => {
    if (GA_TRACKING_ID) {
      gtag('event', eventName, parameters);
    }
  };
  
  return { trackEvent };
};

// Usage in components
const TodoForm = () => {
  const { trackEvent } = useAnalytics();
  
  const handleSubmit = async (todo) => {
    try {
      await createTodo(todo);
      trackEvent('todo_created', {
        priority: todo.priority,
        has_due_date: !!todo.dueDate,
      });
    } catch (error) {
      trackEvent('todo_create_failed', {
        error: error.message,
      });
    }
  };
  
  return (
    // Form JSX
  );
};
```

### Testing Examples
```tsx
import { render, screen, fireEvent, waitFor } from '@testing-library/react';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import TodoForm from './TodoForm';

const createTestQueryClient = () => new QueryClient({
  defaultOptions: {
    queries: {
      retry: false,
    },
    mutations: {
      retry: false,
    },
  },
});

const renderWithProviders = (component) => {
  const testQueryClient = createTestQueryClient();
  return render(
    <QueryClientProvider client={testQueryClient}>
      {component}
    </QueryClientProvider>
  );
};

describe('TodoForm', () => {
  it('renders form fields correctly', () => {
    renderWithProviders(<TodoForm onAdd={jest.fn()} />);
    
    expect(screen.getByLabelText(/title/i)).toBeInTheDocument();
    expect(screen.getByLabelText(/description/i)).toBeInTheDocument();
    expect(screen.getByLabelText(/done/i)).toBeInTheDocument();
    expect(screen.getByRole('button', { name: /add todo/i })).toBeInTheDocument();
  });
  
  it('submits form with correct data', async () => {
    const mockOnAdd = jest.fn();
    const mockAxios = jest.spyOn(axios, 'post').mockResolvedValue({ data: { id: 1, title: 'Test Todo' } });
    
    renderWithProviders(<TodoForm onAdd={mockOnAdd} />);
    
    fireEvent.change(screen.getByLabelText(/title/i), {
      target: { value: 'Test Todo' },
    });
    fireEvent.change(screen.getByLabelText(/description/i), {
      target: { value: 'Test Description' },
    });
    fireEvent.click(screen.getByRole('button', { name: /add todo/i }));
    
    await waitFor(() => {
      expect(mockAxios).toHaveBeenCalledWith('/todos', {
        title: 'Test Todo',
        description: 'Test Description',
        done: false,
      });
      expect(mockOnAdd).toHaveBeenCalledWith({ id: 1, title: 'Test Todo' });
    });
  });
  
  it('shows validation errors for empty title', async () => {
    renderWithProviders(<TodoForm onAdd={jest.fn()} />);
    
    fireEvent.click(screen.getByRole('button', { name: /add todo/i }));
    
    await waitFor(() => {
      expect(screen.getByText(/title is required/i)).toBeInTheDocument();
    });
  });
});
```

### E2E Testing
```tsx
// cypress/integration/todo-app.spec.js
describe('Todo App', () => {
  beforeEach(() => {
    cy.visit('/');
  });
  
  it('loads the app successfully', () => {
    cy.contains('FastAPI + React Todo App').should('be.visible');
  });
  
  it('creates a new todo', () => {
    cy.get('[data-cy="todo-title"]').type('Test Todo');
    cy.get('[data-cy="todo-description"]').type('Test Description');
    cy.get('[data-cy="submit-todo"]').click();
    
    cy.contains('Test Todo').should('be.visible');
    cy.contains('Test Description').should('be.visible');
  });
  
  it('marks todo as complete', () => {
    // Create todo first
    cy.get('[data-cy="todo-title"]').type('Complete Me');
    cy.get('[data-cy="submit-todo"]').click();
    
    // Mark as complete
    cy.contains('Complete Me').parent().find('[data-cy="toggle-todo"]').click();
    
    // Check if it's marked as completed
    cy.contains('Complete Me').should('have.class', 'completed');
  });
  
  it('deletes a todo', () => {
    // Create todo first
    cy.get('[data-cy="todo-title"]').type('Delete Me');
    cy.get('[data-cy="submit-todo"]').click();
    
    // Delete todo
    cy.contains('Delete Me').parent().find('[data-cy="delete-todo"]').click();
    
    // Confirm deletion
    cy.contains('Delete Me').should('not.exist');
  });
  
  it('filters todos by status', () => {
    // Create completed and active todos
    cy.get('[data-cy="todo-title"]').type('Active Todo');
    cy.get('[data-cy="submit-todo"]').click();
    
    cy.get('[data-cy="todo-title"]').type('Completed Todo');
    cy.get('[data-cy="submit-todo"]').click();
    cy.contains('Completed Todo').parent().find('[data-cy="toggle-todo"]').click();
    
    // Filter by active
    cy.get('[data-cy="filter-status"]').select('active');
    cy.contains('Active Todo').should('be.visible');
    cy.contains('Completed Todo').should('not.be.visible');
    
    // Filter by completed
    cy.get('[data-cy="filter-status"]').select('completed');
    cy.contains('Completed Todo').should('be.visible');
    cy.contains('Active Todo').should('not.be.visible');
  });
});
```

### Performance Monitoring
```tsx
import { useEffect } from 'react';

// Web Vitals
import { getCLS, getFID, getFCP, getLCP, getTTFB } from 'web-vitals';

const usePerformanceMonitoring = () => {
  useEffect(() => {
    getCLS(console.log);
    getFID(console.log);
    getFCP(console.log);
    getLCP(console.log);
    getTTFB(console.log);
  }, []);
};

// Error tracking
const useErrorTracking = () => {
  useEffect(() => {
    const handleError = (event) => {
      // Send error to tracking service
      console.error('JavaScript Error:', event.error);
      
      // Example: Send to Sentry, LogRocket, etc.
      // Sentry.captureException(event.error);
    };
    
    const handleUnhandledRejection = (event) => {
      console.error('Unhandled Promise Rejection:', event.reason);
      // Sentry.captureException(event.reason);
    };
    
    window.addEventListener('error', handleError);
    window.addEventListener('unhandledrejection', handleUnhandledRejection);
    
    return () => {
      window.removeEventListener('error', handleError);
      window.removeEventListener('unhandledrejection', handleUnhandledRejection);
    };
  }, []);
};

// Usage
const App = () => {
  usePerformanceMonitoring();
  useErrorTracking();
  
  return (
    // App content
  );
};
```

### Deployment Configuration
```tsx
// vite.config.js
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  build: {
    outDir: 'dist',
    sourcemap: true,
    rollupOptions: {
      output: {
        manualChunks: {
          vendor: ['react', 'react-dom'],
          ui: ['@headlessui/react', 'lucide-react'],
          utils: ['axios', 'date-fns', 'lodash'],
        },
      },
    },
  },
  server: {
    proxy: {
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true,
      },
    },
  },
});
```

### Docker Configuration
```dockerfile
# Dockerfile
FROM node:18-alpine as build

WORKDIR /app

COPY package*.json ./
RUN npm ci --only=production

COPY . .
RUN npm run build

FROM nginx:alpine

COPY --from=build /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
```

```nginx
# nginx.conf
server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    location /api {
        proxy_pass http://backend:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json;
}
```

### CI/CD Pipeline
```yaml
# .github/workflows/deploy.yml
name: Deploy

on:
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Run tests
      run: npm test -- --coverage
    
    - name: Build
      run: npm run build
      
  deploy:
    needs: test
    runs-on: ubuntu-latest
    
    steps:
    - name: Deploy to production
      run: |
        # Deploy commands here
        echo "Deploying to production..."
```

This comprehensive prompt covers all aspects of creating a modern, attractive, and fully-featured Todo application frontend. The implementation includes advanced features like drag-and-drop, offline support, analytics, accessibility, performance optimization, and more. The design focuses on user experience with beautiful UI components, smooth animations, and responsive layouts.